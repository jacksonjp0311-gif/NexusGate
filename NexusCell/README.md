# NexusCell

NexusCell is the execution containment organ of NexusGate.

```text
A sandbox contains code.
NexusCell governs execution.
```

Canonical doctrine:

```text
docs/nexus_cell/NEXUS_CELL_ARCHITECTURE.md
```

Boundary:

```text
This folder is a human-facing organ marker only until implementation is explicitly authorized.
No execution backend is defined here yet.
```



## Desktop Portal Access

```text
NEXUS GATE :: DESKTOP ENTRY PORTAL
[10] NexusCell / Containment -> execution governance doctrine
```

Current line: v0.8.4B portal access and manifest seal.

Boundary: the portal opens doctrine, local folder, GitHub doc, compact laws, and manifest only. It does not execute code.
