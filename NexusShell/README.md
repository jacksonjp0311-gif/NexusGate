# NexusShell

NexusShell is the Python-native no-execution operator shell for NexusGate.

```text
Shell routes.
Compiler validates.
NexusCell plans and bridges context.
Doctor traps wounds.
Human authorizes mutation.
```

Canonical docs:

```text
docs/nexus_shell/NEXUS_SHELL_OPERATOR.md
```

Boundary:

```text
NexusShell is not a backend, sandbox, autonomous repair loop, or arbitrary command executor.
```
