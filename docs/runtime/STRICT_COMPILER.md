# Strict Compiler

The strict compiler is the expanded v0.1.5 gate.

## PowerShell

```powershell
.\scripts\nexus_strict_compile.ps1
```

## Bash

```bash
bash scripts/nexus_strict_compile.sh
```

## Gates

```text
required paths
README trisection
runtime laws
JSON parse
mini README coverage
failure modes
rehydration visibility
update chart current
cold evidence contracts
cold evidence engine
forbidden bypass scan
direct compiler calls
compact commands
Python compile
route contracts
unit tests
ledger append
```

Boundary: a strict compiler pass is a local development gate only.
