# NexusGate Spiral Core Portal v0.1.2

Status: default desktop portal face.

## Close H visual correction

Close G landed the shape and selector alignment, but the live text still used green. You asked for the whole visible letter system to stay in blue and light-blue.

Close H changes the render only:

```text
- keeps the medium title plaque
- keeps the tightened black-hole/event-horizon core
- keeps the selector block left-aligned
- converts visible text palette to blue + light-blue only
- removes green from doctrine and selector text
- retains ASCII-only art for Windows PowerShell safety
- retains exact legacy compatibility markers
```

## Canonical title

```text
NEXUS GATE :: SPIRAL CORE PORTAL
```

## Permanent doctrine lines

```text
WE DO NOT OBEY CHAOS. WE GOVERN THRESHOLDS.
AUTHORITY IS EARNED. ACCESS IS EVIDENCE.
Rule: models recommend; human authorizes durable mutation.
Flow: portal -> surface -> evidence -> gate -> durable commit.
```

## Boundary

This change is visual and navigational only.

It does not change:
- route handlers
- execution authority
- NexusCell containment rules
- model routing
- durable mutation policy
- compiler gates

## Route preservation

All existing routes remain available, including:

```text
[13] Neural Activity / Cathedral
```
