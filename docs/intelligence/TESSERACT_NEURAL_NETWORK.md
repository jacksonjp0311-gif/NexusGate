# Tesseract Neural Network Integration

Adds a minimal root folder `Tesseract Neural Network` and wires it to the NexusGate portal selector, TUI, Electron selector, and NN router role `TNN`.

Boundary: no shell execution, no patch application, no live API pulls, no scraping, no autonomous authority.
