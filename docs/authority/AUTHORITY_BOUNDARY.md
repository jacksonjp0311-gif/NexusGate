# Authority Boundary

Reasoning is not mutation.

Any action that writes, mutates, spends, deletes, sends, deploys, calls tools, changes memory, changes prompts, changes topology, or touches external APIs requires explicit authority verification.

Runtime law:

```text
No authority verification, no mutation.
```