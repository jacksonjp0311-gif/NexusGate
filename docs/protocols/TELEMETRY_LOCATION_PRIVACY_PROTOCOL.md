# Telemetry Location Privacy Protocol

NEXUS must not infer location from IP address, device metadata, repository metadata, or user identity.

Location is explicit and precision-bounded. Default precision is coarse or global.

Exact coordinates are runtime context and must not be committed into canonical source by default.
