# Language Replay Protocol

Language replay starts from durable ledgers and trusted corpus inputs, rebuilds the runtime graph, recomputes hashes, and compares them to the model seal.

No replay, no model seal.
