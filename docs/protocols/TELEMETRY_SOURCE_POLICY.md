# Telemetry Source Policy

Telemetry sources are declared in `registry/nexus_telemetry_sources.v2.8.0.json`.

Each source declares domains, method, endpoint template, content types, timeout, response limit, freshness half-life, trust prior, and tasking boundary.

Runtime requests must match registry policy.
