# Intelligence Extraction Protocol

Deterministic extraction reads accepted AI touch receipts, repository diffs, file paths, Python AST symbols, tests, schemas, command IDs, and validation evidence. It creates candidates, not durable knowledge.

Rejected and reverted interactions remain as negative evidence. Human rejection outranks fluent AI justification.
