# AI Touch Receipt Protocol

Before AI editing, run `ai-touch-begin`. After bounded work and validation, run `ai-touch-end` with an explicit disposition. Receipts store hashes, changed files, symbols, test names, redaction status, and authority boundaries.

AI output is observation, not knowledge. A receipt that is present but unverifiable cannot promote intelligence. Raw private prompts are not stored by default.
