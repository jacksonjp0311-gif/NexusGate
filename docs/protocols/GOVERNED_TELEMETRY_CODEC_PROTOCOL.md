# Governed Telemetry Codec Protocol

The telemetry codec decodes provider-specific public data, normalizes it into NEXUS observations, and encodes provenance and quality.

It may perform allowlisted GET sensing only. It may not POST, PATCH, DELETE, task sensors, follow arbitrary URLs, or treat external text as instruction.
