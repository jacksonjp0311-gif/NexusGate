# Geometric Language Model Protocol

NGLM is a sparse, provenance-preserving language and concept field. It tokenizes locally, builds typed lexical/concept/entity/procedural/self-model layers, propagates bounded activation, and answers only from evidence paths.

Unsupported questions must abstain. Language fluency is not evidence.
